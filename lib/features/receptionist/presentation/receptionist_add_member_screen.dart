import 'package:flutter/material.dart';

import '../../../core/widgets/app_page.dart';

class ReceptionistAddMemberScreen extends StatelessWidget {
  const ReceptionistAddMemberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppPage(title: 'Receptionist Add Member');
  }
}
